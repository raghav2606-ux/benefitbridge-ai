"use client";

import { useState } from "react";
import { ArrowUpRight, BadgeCheck, Building2, CircleGauge, FileText, SearchX, Sparkles } from "lucide-react";
import Card from "@/components/ui/Card";
import Button from "@/components/ui/Button";
import SchemeDetailsModal from "../scheme/SchemeDetailsModal";
import DownloadEligibilityReport from "./DownloadEligibilityReport";
import { EligibilityRequest, EligibleScheme } from "@/types/eligibility";

interface Props { schemes: EligibleScheme[]; profile?: EligibilityRequest | null; }

export default function RecommendationList({ schemes, profile }: Props) {
  const [selectedScheme, setSelectedScheme] = useState<EligibleScheme | null>(null);
  const [open, setOpen] = useState(false);
  const openModal = (scheme: EligibleScheme) => { setSelectedScheme(scheme); setOpen(true); };
  const closeModal = () => { setOpen(false); setSelectedScheme(null); };

  if (schemes.length === 0) return <section className="mt-12 overflow-hidden rounded-3xl border border-dashed border-slate-300 bg-gradient-to-br from-slate-50 to-blue-50 p-8 text-center shadow-sm dark:border-slate-700 dark:from-slate-900 dark:to-blue-950/30 sm:p-12"><div className="mx-auto flex h-14 w-14 items-center justify-center rounded-2xl bg-white shadow-md dark:bg-slate-800"><SearchX className="h-7 w-7 text-blue-600" /></div><h2 className="mt-5 text-2xl font-extrabold tracking-tight text-slate-900 dark:text-white">No matches yet</h2><p className="mx-auto mt-3 max-w-md leading-7 text-slate-600 dark:text-slate-300">Try checking your state, category, education details, and occupation. Scheme criteria can be specific.</p></section>;

  return <><section className="mt-14"><div className="mb-8 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><div className="inline-flex items-center gap-2 rounded-full bg-blue-50 px-3 py-1 text-xs font-bold uppercase tracking-wide text-blue-700 dark:bg-blue-950/60 dark:text-blue-200"><Sparkles className="h-3.5 w-3.5" /> Personalized results</div><h2 className="mt-3 text-3xl font-extrabold tracking-tight text-slate-900 dark:text-white sm:text-4xl">Your eligible schemes</h2><p className="mt-2 text-slate-600 dark:text-slate-300">{schemes.length} scheme{schemes.length === 1 ? "" : "s"} matches the details you provided.</p></div><div className="flex flex-col gap-2 self-start sm:items-end"><div className="inline-flex items-center gap-2 rounded-xl border border-emerald-100 bg-emerald-50 px-3 py-2 text-sm font-semibold text-emerald-700 dark:border-emerald-900/70 dark:bg-emerald-950/40 dark:text-emerald-300"><BadgeCheck className="h-4 w-4" /> Criteria checked</div>{profile && <DownloadEligibilityReport profile={profile} schemes={schemes} />}</div></div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{schemes.map((scheme) => <Card key={scheme.scheme_id} className="group relative flex h-full flex-col overflow-hidden p-0"><div className="h-1.5 bg-gradient-to-r from-blue-600 via-indigo-500 to-cyan-400" /><div className="flex flex-1 flex-col p-6"><div className="flex items-start justify-between gap-3"><span className="rounded-full bg-blue-50 px-3 py-1.5 text-xs font-bold text-blue-700 dark:bg-blue-950/60 dark:text-blue-200">{scheme.category}</span><span className="inline-flex items-center gap-1 text-xs font-medium text-slate-500 dark:text-slate-400"><Building2 className="h-3.5 w-3.5" />{scheme.government_level}</span></div><div className="mt-5 flex items-start justify-between gap-4"><h3 className="text-xl font-extrabold leading-7 tracking-tight text-slate-900 dark:text-white">{scheme.scheme_name}</h3><div className="flex shrink-0 flex-col items-center rounded-2xl bg-emerald-50 px-2.5 py-2 text-emerald-700 dark:bg-emerald-950/40 dark:text-emerald-300"><CircleGauge className="h-4 w-4" /><span className="mt-1 text-xs font-black">{scheme.eligibility_score}</span></div></div><p className="mt-3 line-clamp-3 text-sm leading-6 text-slate-600 dark:text-slate-300">{scheme.summary}</p><div className="mt-5 rounded-2xl border border-blue-100 bg-gradient-to-r from-blue-50 to-indigo-50 p-4 dark:border-blue-900/60 dark:from-blue-950/40 dark:to-indigo-950/30"><p className="text-xs font-bold uppercase tracking-wide text-blue-700 dark:text-blue-200">Estimated benefit</p><p className="mt-1 text-lg font-extrabold text-slate-900 dark:text-white">{scheme.estimated_benefit}</p></div><div className="mt-4 flex items-center justify-between gap-3 text-sm"><span className="inline-flex items-center gap-2 font-semibold text-emerald-700 dark:text-emerald-300"><FileText className="h-4 w-4" />{scheme.required_documents.length} documents</span><span className="text-slate-500 dark:text-slate-400">{scheme.processing_time}</span></div><div className="mt-6 flex gap-3 border-t border-slate-100 pt-5 dark:border-slate-800"><Button variant="outline" className="flex-1" onClick={() => openModal(scheme)}>Details</Button><a href={scheme.application.official_url} target="_blank" rel="noopener noreferrer" className="inline-flex min-h-11 flex-1 items-center justify-center gap-1 rounded-xl bg-gradient-to-r from-blue-600 to-indigo-600 px-4 text-sm font-semibold text-white shadow-lg shadow-blue-600/20 transition duration-200 hover:-translate-y-0.5 hover:scale-[1.02] hover:shadow-xl"><span>Apply</span><ArrowUpRight className="h-4 w-4" /></a></div></div></Card>)}</div></section><SchemeDetailsModal scheme={selectedScheme} open={open} onClose={closeModal} /></>;
}
