import { ReactNode } from "react";
import { ArrowRight, BadgeCheck, BrainCircuit, CheckCircle2, Clock3, IndianRupee, ListChecks, Sparkles } from "lucide-react";
import { EligibleScheme } from "@/types/eligibility";

interface Props { scheme: EligibleScheme; }

function scoreColor(score: number) {
  if (score >= 80) return "#16a34a";
  if (score >= 60) return "#2563eb";
  return "#f59e0b";
}

export default function AIDashboard({ scheme }: Props) {
  const score = Math.max(0, Math.min(100, scheme.eligibility_score));
  const confidence = scheme.eligibility_confidence ?? scheme.ai_explanation.confidence;
  const nextSteps = scheme.recommended_next_steps ?? scheme.action_plan;
  const scoreStroke = scoreColor(score);

  return <section aria-labelledby="eligibility-dashboard-title" className="mt-8 overflow-hidden rounded-3xl border border-indigo-100 bg-gradient-to-br from-indigo-50 via-white to-cyan-50 shadow-xl shadow-indigo-900/5"><div className="border-b border-indigo-100 bg-white/70 px-6 py-5 backdrop-blur sm:px-8"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><div className="inline-flex items-center gap-2 rounded-full bg-indigo-50 px-3 py-1 text-xs font-bold uppercase tracking-wide text-indigo-700"><BrainCircuit className="h-3.5 w-3.5" /> Eligibility intelligence</div><h3 id="eligibility-dashboard-title" className="mt-2 text-2xl font-extrabold tracking-tight text-slate-900">Your eligibility dashboard</h3></div><div className="inline-flex items-center gap-2 self-start rounded-xl border border-emerald-100 bg-emerald-50 px-3 py-2 text-sm font-bold text-emerald-700"><BadgeCheck className="h-4 w-4" /> {confidence}% confidence</div></div></div><div className="p-6 sm:p-8"><div className="grid gap-6 lg:grid-cols-[0.86fr_1.14fr]"><div className="rounded-3xl bg-slate-950 p-6 text-white shadow-xl shadow-indigo-950/20"><p className="text-sm font-bold uppercase tracking-wider text-blue-200">Eligibility score</p><div className="mx-auto mt-6 flex h-44 w-44 items-center justify-center rounded-full p-3" style={{ background: `conic-gradient(${scoreStroke} ${score * 3.6}deg, rgba(255,255,255,0.16) 0deg)` }}><div className="flex h-full w-full flex-col items-center justify-center rounded-full bg-slate-950"><span className="text-5xl font-black tracking-tight">{score}</span><span className="mt-1 text-xs font-bold uppercase tracking-widest text-slate-400">out of 100</span></div></div><div className="mt-6 rounded-2xl border border-white/10 bg-white/10 p-4"><p className="text-sm font-semibold text-slate-200">Eligibility confidence</p><div className="mt-3 h-2 overflow-hidden rounded-full bg-white/15"><div className="h-full rounded-full bg-gradient-to-r from-cyan-300 to-emerald-300" style={{ width: `${confidence}%` }} /></div><p className="mt-2 text-xs leading-5 text-slate-400">Based on the profile details and recorded scheme criteria.</p></div></div><div className="grid gap-4 sm:grid-cols-2"><Metric icon={<IndianRupee className="h-5 w-5" />} label="Benefit estimate" value={scheme.estimated_benefit} accent="blue" /><Metric icon={<Clock3 className="h-5 w-5" />} label="Processing time" value={scheme.processing_time} accent="violet" /><Metric icon={<Sparkles className="h-5 w-5" />} label="Application difficulty" value={scheme.difficulty} accent="amber" /><Metric icon={<ListChecks className="h-5 w-5" />} label="Documents needed" value={`${scheme.required_documents.length} required`} accent="emerald" /></div></div><div className="mt-7 grid gap-6 lg:grid-cols-2"><div className="rounded-2xl border border-indigo-100 bg-white p-5 shadow-sm"><h4 className="flex items-center gap-2 font-extrabold text-slate-900"><BrainCircuit className="h-5 w-5 text-indigo-600" /> AI explanation</h4><p className="mt-3 leading-7 text-slate-600">{scheme.ai_explanation.summary}</p><h5 className="mt-5 text-sm font-extrabold uppercase tracking-wide text-slate-500">Why you are eligible</h5><ul className="mt-3 space-y-3">{scheme.ai_explanation.why_eligible.map((reason) => <li key={reason} className="flex gap-3 text-sm leading-6 text-slate-700"><CheckCircle2 className="mt-0.5 h-4 w-4 shrink-0 text-emerald-600" />{reason}</li>)}</ul></div><div className="rounded-2xl border border-cyan-100 bg-white p-5 shadow-sm"><h4 className="flex items-center gap-2 font-extrabold text-slate-900"><ListChecks className="h-5 w-5 text-cyan-600" /> Recommended next steps</h4><ol className="mt-4 space-y-4">{nextSteps.map((step, index) => <li key={step} className="flex gap-3 text-sm leading-6 text-slate-700"><span className="flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-cyan-50 text-xs font-extrabold text-cyan-700">{index + 1}</span>{step}</li>)}</ol><div className="mt-5 flex items-center gap-2 rounded-xl bg-cyan-50 p-3 text-sm font-semibold text-cyan-800"><ArrowRight className="h-4 w-4" /> Continue with the official application when ready.</div></div></div><div className="mt-7"><h4 className="text-sm font-extrabold uppercase tracking-wide text-slate-500">Eligibility breakdown</h4><div className="mt-3 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{scheme.eligibility_breakdown.map((item) => <div key={item.title} className="rounded-xl border border-slate-100 bg-white p-4 shadow-sm"><div className="flex items-center justify-between gap-2"><span className="text-sm font-bold text-slate-700">{item.title}</span><span className={item.matched ? "text-emerald-600" : "text-amber-600"}>{item.matched ? <CheckCircle2 className="h-4 w-4" /> : "—"}</span></div><div className="mt-3 h-1.5 overflow-hidden rounded-full bg-slate-100"><div className={item.matched ? "h-full rounded-full bg-emerald-500" : "h-full rounded-full bg-amber-400"} style={{ width: `${item.score}%` }} /></div><p className="mt-2 text-xs font-medium text-slate-500">{item.matched ? "Matched" : "Needs review"} · {item.score} points</p></div>)}</div></div></div></section>;
}

function Metric({ icon, label, value, accent }: { icon: ReactNode; label: string; value: string; accent: "blue" | "violet" | "amber" | "emerald"; }) {
  const accents = { blue: "bg-blue-50 text-blue-700", violet: "bg-violet-50 text-violet-700", amber: "bg-amber-50 text-amber-700", emerald: "bg-emerald-50 text-emerald-700" };
  return <div className="rounded-2xl border border-slate-100 bg-white p-5 shadow-sm"><div className={`inline-flex rounded-xl p-2.5 ${accents[accent]}`}>{icon}</div><p className="mt-4 text-sm font-semibold text-slate-500">{label}</p><p className="mt-1 text-lg font-extrabold tracking-tight text-slate-900">{value}</p></div>;
}
