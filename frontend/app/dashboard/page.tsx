"use client";

import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import { ArrowRight, BadgeCheck, CalendarDays, ClipboardCheck, FileText, FolderSearch, GitCompareArrows, HeartHandshake, LayoutDashboard, ListChecks, Search, Sparkles } from "lucide-react";
import { getEligibilityHistory, EligibilityHistory } from "@/lib/eligibilityHistory";
import Button from "@/components/ui/Button";
import Card from "@/components/ui/Card";
import Container from "@/components/ui/Container";
import DownloadEligibilityReport from "@/components/recommendations/DownloadEligibilityReport";

function formatDate(date: string) {
  const parsed = new Date(date);
  return Number.isNaN(parsed.getTime()) ? "Not available" : new Intl.DateTimeFormat("en-IN", { dateStyle: "medium", timeStyle: "short" }).format(parsed);
}

export default function DashboardPage() {
  const [history, setHistory] = useState<EligibilityHistory | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    const timeoutId = window.setTimeout(() => {
      setHistory(getEligibilityHistory());
      setLoading(false);
    }, 0);
    return () => window.clearTimeout(timeoutId);
  }, []);

  const categoriesMatched = useMemo(() => history ? new Set(history.schemes.map((scheme) => scheme.category)).size : 0, [history]);
  const topScheme = history?.schemes[0];
  const summaryCards = [
    { label: "Total Recommended Schemes", value: history?.schemes.length ?? 0, detail: history ? "From your latest check" : "Run an eligibility check to begin", icon: ListChecks, accent: "text-blue-600 dark:text-blue-300", surface: "bg-blue-50 dark:bg-blue-950/40" },
    { label: "Categories Matched", value: categoriesMatched, detail: history ? "Across your recommendations" : "Matches will appear here", icon: FolderSearch, accent: "text-indigo-600 dark:text-indigo-300", surface: "bg-indigo-50 dark:bg-indigo-950/40" },
    { label: "Profile Status", value: history ? "Saved locally" : "Not checked", detail: history ? "Stored only in this browser" : "No profile information saved", icon: BadgeCheck, accent: "text-emerald-600 dark:text-emerald-300", surface: "bg-emerald-50 dark:bg-emerald-950/40" },
    { label: "Last Eligibility Check", value: history ? formatDate(history.checkedAt) : "Not available", detail: history ? "Latest result shown below" : "Complete your first check", icon: CalendarDays, accent: "text-cyan-600 dark:text-cyan-300", surface: "bg-cyan-50 dark:bg-cyan-950/40" },
  ];

  return <Container className="py-12 sm:py-20"><div className="max-w-3xl"><span className="inline-flex items-center gap-2 rounded-full bg-blue-50 px-3 py-1 text-xs font-bold uppercase tracking-wide text-blue-700 dark:bg-blue-950/60 dark:text-blue-200"><LayoutDashboard className="h-3.5 w-3.5" /> Personal dashboard</span><h1 className="mt-4 text-4xl font-extrabold tracking-tight text-slate-900 dark:text-white sm:text-5xl">Welcome to BenefitBridge AI.</h1><p className="mt-4 text-lg leading-8 text-slate-600 dark:text-slate-300">Keep track of your latest eligibility results, recommended schemes, and next actions in one place.</p></div><section className="mt-10 grid gap-5 sm:grid-cols-2 xl:grid-cols-4" aria-label="Dashboard summary">{summaryCards.map((card) => <Card key={card.label} className="min-h-44" hover={false}><div className={`inline-flex rounded-xl p-2.5 ${card.surface}`}><card.icon className={`h-5 w-5 ${card.accent}`} /></div><p className="mt-5 text-sm font-bold text-slate-500 dark:text-slate-400">{card.label}</p><p className="mt-2 text-xl font-extrabold tracking-tight text-slate-900 dark:text-white">{loading ? "Loading…" : card.value}</p><p className="mt-2 text-xs leading-5 text-slate-500 dark:text-slate-400">{card.detail}</p></Card>)}</section>{!loading && !history && <Card className="mt-10 overflow-hidden p-0" hover={false}><div className="flex flex-col gap-6 bg-gradient-to-br from-blue-50 via-white to-indigo-50 p-7 dark:from-blue-950/40 dark:via-slate-900 dark:to-indigo-950/30 sm:p-10"><div className="flex h-14 w-14 items-center justify-center rounded-2xl bg-white shadow-md dark:bg-slate-800"><HeartHandshake className="h-7 w-7 text-blue-600 dark:text-blue-400" /></div><div><h2 className="text-2xl font-extrabold tracking-tight text-slate-900 dark:text-white">Start with your first eligibility check</h2><p className="mt-3 max-w-2xl leading-7 text-slate-600 dark:text-slate-300">Tell us about your situation and we’ll save your latest recommendations in this browser, so you can revisit them here whenever you need.</p></div><Link href="/eligibility" className="self-start"><Button type="button"><ClipboardCheck className="h-4 w-4" /> Check Eligibility <ArrowRight className="h-4 w-4" /></Button></Link></div></Card>}{!loading && history && <><section className="mt-10 grid gap-5 lg:grid-cols-[1.15fr_0.85fr]"><Card className="overflow-hidden p-0" hover={false}>{topScheme ? <div className="p-6 sm:p-8"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><div className="inline-flex items-center gap-2 rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold uppercase tracking-wide text-emerald-700 dark:bg-emerald-950/50 dark:text-emerald-300"><Sparkles className="h-3.5 w-3.5" /> Top recommendation</div><h2 className="mt-4 text-2xl font-extrabold tracking-tight text-slate-900 dark:text-white">{topScheme.scheme_name}</h2><p className="mt-2 text-sm font-semibold text-blue-700 dark:text-blue-300">{topScheme.category}</p></div><div className="rounded-2xl bg-emerald-50 px-4 py-3 text-center dark:bg-emerald-950/40"><p className="text-xs font-bold uppercase tracking-wide text-emerald-700 dark:text-emerald-300">Match</p><p className="mt-1 text-2xl font-black text-emerald-700 dark:text-emerald-300">{topScheme.eligibility_score}%</p></div></div><p className="mt-5 leading-7 text-slate-600 dark:text-slate-300">{topScheme.summary}</p><div className="mt-6 border-t border-slate-100 pt-5 dark:border-slate-800"><p className="text-sm font-extrabold text-slate-900 dark:text-white">Required documents</p><div className="mt-3 flex flex-wrap gap-2">{topScheme.required_documents.map((document) => <span key={document} className="inline-flex items-center gap-1.5 rounded-full bg-slate-100 px-3 py-1.5 text-xs font-semibold text-slate-700 dark:bg-slate-800 dark:text-slate-200"><FileText className="h-3.5 w-3.5 text-blue-600 dark:text-blue-400" /> {document}</span>)}</div></div><p className="mt-6 text-xs text-slate-500 dark:text-slate-400">Recommendation date: {formatDate(history.checkedAt)}</p></div> : <div className="p-8"><h2 className="text-2xl font-extrabold text-slate-900 dark:text-white">No schemes matched yet</h2><p className="mt-3 leading-7 text-slate-600 dark:text-slate-300">Try updating your profile details and checking eligibility again.</p></div>}</Card><Card hover={false}><div className="inline-flex rounded-xl bg-indigo-50 p-2.5 dark:bg-indigo-950/40"><ClipboardCheck className="h-5 w-5 text-indigo-600 dark:text-indigo-300" /></div><h2 className="mt-5 text-xl font-extrabold text-slate-900 dark:text-white">Latest check</h2><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-300">Your latest recommendations are saved locally on this device. Run another check whenever your details change.</p><dl className="mt-6 space-y-3 border-t border-slate-100 pt-5 text-sm dark:border-slate-800"><div className="flex items-center justify-between gap-4"><dt className="text-slate-500 dark:text-slate-400">Profile</dt><dd className="font-bold text-slate-900 dark:text-white">{history.profile.occupation || "General profile"}</dd></div><div className="flex items-center justify-between gap-4"><dt className="text-slate-500 dark:text-slate-400">State</dt><dd className="font-bold text-slate-900 dark:text-white">{history.profile.state}</dd></div><div className="flex items-center justify-between gap-4"><dt className="text-slate-500 dark:text-slate-400">Checked</dt><dd className="font-bold text-slate-900 dark:text-white">{formatDate(history.checkedAt)}</dd></div></dl></Card></section><section className="mt-12"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><h2 className="text-2xl font-extrabold tracking-tight text-slate-900 dark:text-white">Recent recommendations</h2><p className="mt-2 text-slate-600 dark:text-slate-300">The latest schemes that matched your eligibility profile.</p></div><Link href="/schemes" className="inline-flex items-center gap-1 font-bold text-blue-700 transition hover:text-indigo-700 dark:text-blue-300 dark:hover:text-blue-200">Browse all schemes <ArrowRight className="h-4 w-4" /></Link></div>{history.schemes.length > 0 ? <div className="mt-6 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{history.schemes.slice(0, 3).map((scheme) => <Card key={scheme.scheme_id} className="flex min-h-64 flex-col"><div className="flex items-start justify-between gap-3"><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-bold text-blue-700 dark:bg-blue-950/60 dark:text-blue-200">{scheme.category}</span><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-black text-emerald-700 dark:bg-emerald-950/40 dark:text-emerald-300">{scheme.eligibility_score}% match</span></div><h3 className="mt-5 text-xl font-extrabold tracking-tight text-slate-900 dark:text-white">{scheme.scheme_name}</h3><p className="mt-3 flex-1 text-sm leading-6 text-slate-600 dark:text-slate-300">{scheme.summary}</p><p className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-slate-500 dark:text-slate-400"><FileText className="h-4 w-4 text-blue-600 dark:text-blue-400" /> {scheme.required_documents.length} documents required</p></Card>)}</div> : <Card className="mt-6" hover={false}><p className="font-bold text-slate-900 dark:text-white">No recommendations matched this profile.</p><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-300">Update your details and run another eligibility check to find available schemes.</p></Card>}</section></>}<section className="mt-12"><div><h2 className="text-2xl font-extrabold tracking-tight text-slate-900 dark:text-white">Quick actions</h2><p className="mt-2 text-slate-600 dark:text-slate-300">Continue with the next step that is useful to you.</p></div><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Link href="/eligibility"><Card className="h-full" hover><ClipboardCheck className="h-6 w-6 text-blue-600 dark:text-blue-400" /><h3 className="mt-4 font-extrabold text-slate-900 dark:text-white">Check Eligibility</h3><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-300">Update your profile and get fresh matches.</p></Card></Link><Link href="/schemes"><Card className="h-full" hover><Search className="h-6 w-6 text-indigo-600 dark:text-indigo-400" /><h3 className="mt-4 font-extrabold text-slate-900 dark:text-white">Browse Schemes</h3><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-300">Explore official government scheme options.</p></Card></Link><Link href="/compare"><Card className="h-full" hover><GitCompareArrows className="h-6 w-6 text-cyan-600 dark:text-cyan-400" /><h3 className="mt-4 font-extrabold text-slate-900 dark:text-white">Compare Schemes</h3><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-300">Review two saved schemes side by side.</p></Card></Link>{history ? <div><DownloadEligibilityReport profile={history.profile} schemes={history.schemes} /></div> : <Link href="/eligibility"><Card className="h-full" hover><FileText className="h-6 w-6 text-emerald-600 dark:text-emerald-400" /><h3 className="mt-4 font-extrabold text-slate-900 dark:text-white">Download PDF Report</h3><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-300">Complete an eligibility check first to create your report.</p></Card></Link>}</div></section></Container>;
}
